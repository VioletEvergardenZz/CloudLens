# 阶段回归报告（2026-03-10）

> 文档状态：兼容保留  
> 校准日期：2026-03-02  
> 口径说明：本报告对应阶段 A 收口周结，本文档为 2026-03-02 预执行结果归档。

- 报告日期：2026-03-10（预执行）
- 实际执行日期：2026-03-02
- 执行人：Codex（结对执行）
- 环境：`dev`
- 版本：`main`（本地工作区）

## 1. 目标与范围

- 本轮目标：完成阶段 A 收口计划中工作包 1~4 与周结产物，验证“文件入云 + AI 分析 + 知识库回滚 + 控制台闭环 + 最小安全门禁”可连续回归。
- 覆盖链路：
  - 文件入云双轮回归（`2026-03-02`、`2026-03-03`）
  - AI 周度回放与基线（`2026-03-04`）
  - 知识库失败与回滚演练（`2026-03-07`）
  - 控制台闭环与降级接口验收（`2026-03-07`）
  - 后端测试与前端构建门禁

## 2. 执行清单

| 项目 | 命令/入口 | 结果（P/F） | 备注 |
| --- | --- | --- | --- |
| 后端测试 | `cd go-watch-file && go test ./... -count=1` | P | 全量通过 |
| 前端构建 | `source /etc/profile && cd console-frontend && npm run build` | P | 构建通过（存在 chunk size 告警） |
| 上传双轮回归 | `upload-recap-lite.sh` | P | `upload-recap-2026-03-02/03` 均 `allPassed=true` |
| AI 周度回放 | `ai-weekly-recap.sh --date 2026-03-04` | P | `15` 条样本，三类失败场景覆盖 |
| 知识库失败回滚演练 | 手册 A/B/C 三场景复核 | P | `kb-failure-rollback-drill-2026-03-07.json` |
| 控制台闭环验收 | `console-closure-check.sh` | P | `/api/dashboard`、`/api/dashboard?mode=light` 均 `200` |
| 阶段轻量复盘 | `stage-recap-lite.sh` | P | `stage-recap-2026-03-10-lite.json` `pass=true` |

## 3. 关键指标结果

| 指标 | 目标 | 本次结果 | 是否达标 |
| --- | --- | --- | --- |
| `gwf_upload_queue_full_total` 增量 | `= 0` | 双轮均为 `0` | 是 |
| 上传失败率（10m） | `< 5%` | 双轮均为 `0%` | 是 |
| AI 降级率 | `<= 20%` | `0.20` | 是（贴边） |
| AI 结构通过率 | `= 1.00` | `1.00` | 是 |
| AI 错误分类覆盖率 | `= 1.00` | `1.00` | 是 |
| 知识库失败回滚演练 | `pass=true` | `true` | 是 |
| 控制台闭环验收 | `pass=true` | `true` | 是 |
| `/api/health` `/api/dashboard` `/api/dashboard?mode=light` | `200` | 均 `200` | 是 |

## 4. 失败样例与根因

| 场景 | 现象 | 根因 | 修复动作 | Owner | 截止日期 |
| --- | --- | --- | --- | --- | --- |
| 上传队列保护触发 | 压测中 `queueShedDelta` 增量（931/962） | 高压瞬时流量触发队列保护策略 | 保留保护策略并在周结持续对比波动 | 平台后端 | 下轮周结 |
| AI 降级率贴边 | `degradedRatio=0.20` 达到阈值上限 | 本轮主动注入 timeout/network/upstream_5xx 演练样本 | 持续补充真实故障样本并观察阈值稳定性 | AI/运维 | 下轮周结 |

## 5. 风险与待办

1. 风险：AI 降级率已经贴近门禁阈值，后续真实故障波动可能导致门禁失败。  
2. 风险：当前三类失败样本包含演练注入路径，真实线上样本仍需持续补齐。  
3. 待办：按计划下一轮继续复跑 AI 与知识库演练，并更新周结对比曲线。

## 6. 结论

- 是否允许进入下一阶段：`是（仅作为 P1 立项输入，不提前开发 P1 能力）`
- 结论摘要：阶段 A 收口计划的核心动作已完成并形成可追溯产物；后端测试、前端构建、AI 周度基线、知识库回滚与控制台闭环均通过当前门禁，具备阶段收口条件。
