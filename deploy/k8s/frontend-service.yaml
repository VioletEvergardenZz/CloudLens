apiVersion: v1
kind: Service
metadata:
  name: cloudlens-frontend
  namespace: cloudlens
  labels:
    app.kubernetes.io/name: cloudlens
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cloudlens
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http

