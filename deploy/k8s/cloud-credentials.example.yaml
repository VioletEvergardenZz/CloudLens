apiVersion: v1
kind: Secret
metadata:
  name: cloudlens-cloud-credentials
  namespace: cloudlens
type: Opaque
stringData:
  ALIYUN_ACCESS_KEY_ID: ""
  ALIYUN_ACCESS_KEY_SECRET: ""
  HUAWEI_ACCESS_KEY_ID: ""
  HUAWEI_ACCESS_KEY_SECRET: ""
  # 地域变量只在需要临时限定范围时再显式添加，默认会自动发现全部可见地域。
  # ALIYUN_REGIONS: "cn-hangzhou"
  # HUAWEI_REGIONS: "cn-south-1"
  AI_API_KEY: ""
